#!/bin/bash

#DIRECTIVES:

#PBS -N matrixex_SpMV_benchmark

#PBS -o ../results/log.out
#PBS -e ../results/log.err

#PBS -q short_cpuQ
#PBS -l walltime=06:00:00
#PBS -l select=1:ncpus=64:mem=16gb

module load gcc91
module load perf

 #going to the main directory of the deliverable
cd "$PBS_O_WORKDIR/.."
cd "src"

mkdir -p ../data

PERF_MODE=0
PERF_RESULTS_FILE="../results/perf_results_logs.csv"
PERF_TIME_RESULTS_FILE="../results/perf_time_results.csv"

while getopts "p" opt; do
  case $opt in
    p)
      echo "Flag -p rilevato: Attivazione modalità PERF"
      PERF_MODE=1 
      ;;
    \?)
      echo "Errore: Opzione -$OPTARG non valida." >&2
      exit 1
      ;;
  esac
done

#download matrices

matrix_download(){

	local NAME=$1
	local URL=$2
	local TARGET_DIR="../data"
	local CHECK_FILE="$TARGET_DIR/$NAME.mtx"

	if [ -f "$CHECK_FILE" ]; then
		echo "Matrix $NAME found! No download needed"
	else
		echo "Matrix not founded. Starting to download..."

		wget -q --show-progress "$URL" -O "$TARGET_DIR/$NAME.tar.gz"

		if [ $? -ne 0 ]; then
			echo "ERROR: download $NAME failed!"
			exit 1
		fi

		echo "Starting extraction of $NAME..."
		tar -xzf "$TARGET_DIR/$NAME.tar.gz" -C "$TARGET_DIR"
		        
	    if [ -f "$TARGET_DIR/$NAME/$NAME.mtx" ]; then
		  	mv "$TARGET_DIR/$NAME/$NAME.mtx" "$TARGET_DIR/"
		    rm -rf "$TARGET_DIR/$NAME" 
		    rm "$TARGET_DIR/$NAME.tar.gz" 
		    echo "Matrice $NAME pronta all'uso."
		else
		    echo "ERRORE: File .mtx non trovato dopo l'estrazione per $NAME"
		    exit 1
		fi
		
	fi
	
}


echo "Checking matrices..."

matrix_download "Freescale2" "https://sparse.tamu.edu/MM/Freescale/Freescale2.tar.gz"
matrix_download "rajat31" "https://sparse.tamu.edu/MM/Rajat/rajat31.tar.gz"
matrix_download "G3_circuit" "https://sparse.tamu.edu/MM/AMD/G3_circuit.tar.gz"

echo "Compiling and executing benchmark..."


gcc -O3 -fopenmp -I../include -o spmv_benchmark main.c mmio.c
if [ $? -ne 0 ]; then
	echo "ERR: compilation failed!"
	exit 1
fi
echo "Compilation completed!"

#definition of all the combinations

MATRICES=(	"../data/Freescale2.mtx"
			"../data/rajat31.mtx"
			"../data/G3_circuit.mtx"	)

THREADS=(1 2 4 8 16 32 64)
SCHEDULES=(static dynamic guided)
CHUNKS=(1 10 100 1000 10000)

RESULTS_FILE="../results/"




echo "Starting benchmark..."

if [ "$PERF_MODE" -eq 0 ]; then

	echo "matrix, threads, schedules type, chunk size, time (ms)" > "$RESULTS_FILE/time_results.csv"

	for matrix in "${MATRICES[@]}"; do
		for threads in "${THREADS[@]}"; do
			for schedule in "${SCHEDULES[@]}"; do
				for chunk in "${CHUNKS[@]}"; do
					if [ "$threads" -eq 1 ] && { [ "$schedule" != "static" ] || [ "$chunk" -ne 1 ]; }; then
						continue
					fi
									
					echo "Running 10 times: $matrix, $threads, $schedule, $chunk"
					
					
						./spmv_benchmark "$matrix" "$threads" "$schedule" "$chunk"
					                    
					    if [ $? -ne 0 ]; then
					    	echo "ERRORE durante l'esecuzione (Run $i): $matrix, $threads, $schedule, $chunk"
					    fi
					
				done
			done
		done
	done

else

	echo "matrix, threads, schedules type, chunk size, time (ms)" > "$PERF_TIME_RESULTS_FILE"
	echo " " > "$PERF_RESULTS_FILE"

	for matrix in "${MATRICES[@]}"; do
		for threads in "${THREADS[@]}"; do
			for schedule in "${SCHEDULES[@]}"; do
				for chunk in "${CHUNKS[@]}"; do

					if [ "$threads" -eq 1 ] && { [ "$schedule" != "static" ] || [ "$chunk" -ne 1 ]; }; then
						continue
					fi
									
					echo "Running perf 5 times: $matrix, $threads, $schedule, $chunk"
					echo "$matrix,$threads,$schedule,$chunk" >> "$PERF_RESULTS_FILE"
					
					for ((i=0; i<5; i++)); do
						perf stat -x, -e L1-dcache-loads,L1-dcache-load-misses,LLC-loads,LLC-misses ./spmv_benchmark "$matrix" "$threads" "$schedule" "$chunk" "$PERF_TIME_RESULTS_FILE" 2>> "$PERF_RESULTS_FILE"					                    
					    if [ $? -ne 0 ]; then
					    	echo "ERRORE durante l'esecuzione perf (Run $i): $matrix, $threads, $schedule, $chunk"
					    fi
					done
				done
			done
		done
	done

fi

echo "Benchmark completed. Time results saved in $RESULTS_FILE"

exit
